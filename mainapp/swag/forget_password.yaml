API for forget password
---
tags:
  - User
parameters:
  - in: body
    name: data
    schema:
      $ref: '#/definitions/ForgetPassword'
responses:
  201:
    description: Successfully updated user password
    schema:
      $ref: '#/definitions/ForgetPassword'
definitions:
  ForgetPassword:
    properties:
      email_id:
        type: string
      password:
        type: string
